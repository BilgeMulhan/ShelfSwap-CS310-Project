import 'package:flutter/material.dart';

class AppPaddings {
  static const EdgeInsets screen = EdgeInsets.symmetric(horizontal: 20, vertical: 16);
  static const EdgeInsets card = EdgeInsets.all(12);
  static const EdgeInsets input = EdgeInsets.symmetric(horizontal: 14, vertical: 14);
}